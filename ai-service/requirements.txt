# Runtime
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
httpx>=0.27
python-dotenv>=1.0
huggingface-hub>=0.24

# PDF page rendering for scanned-PDF question extraction (vision path)
pypdfium2>=4.30
pillow>=10.0

# Optional — only needed for AI_BACKEND=transformers (in-process models):
#   pip install "transformers>=4.44" torch accelerate
